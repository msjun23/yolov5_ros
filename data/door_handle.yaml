path: ../datasets/door_handle_nuri_sim  # dataset root dir
train: train/images  # train images (relative to 'path')
val: valid/images    # validation images (relative to 'path')

nc: 2
names: ['door', 'handle']

